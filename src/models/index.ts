import { UserDocument, UserModel, UserSchema } from './User';
import { NoteDocument, NoteModel, NoteSchema } from './Note';

/**
 *
 *
 *
 */

export {
  // user
  UserDocument,
  UserModel,
  UserSchema,

  // note
  NoteDocument,
  NoteModel,
  NoteSchema,
};
